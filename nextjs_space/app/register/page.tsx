import { RegisterClient } from './register-client';

export const metadata = {
  title: 'Create Church Account | Ministerial Worship Planner',
  description: 'Register your church and start managing worship programs',
};

export default function RegisterPage() {
  return <RegisterClient />;
}
