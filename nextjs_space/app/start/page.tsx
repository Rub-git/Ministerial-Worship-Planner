import { StartClient } from './start-client';

export const metadata = {
  title: 'Select Your Church Tradition | Ministerial Worship Planner',
  description: 'Choose your denomination to customize templates and doctrinal settings for your church.',
};

export default function StartPage() {
  return <StartClient />;
}
